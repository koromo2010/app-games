{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_PROJECT_NAME\" = \"app-games-sdk\" ]; then if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"develop\" ]; then exit 0; else exit 1; fi; else if [ \"$VERCEL_GIT_COMMIT_REF\" != \"develop\" ]; then exit 0; else exit 1; fi; fi"
}
