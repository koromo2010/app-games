export default function Home() {
  return (
    <main>
      <section className="status">
        <code>GAME FIELDS / ISOLATED PREVIEW</code>
        <h1>Preview runtime is ready</h1>
        <p>このオリジンは未審査の画面モックを隔離表示するためだけに使われます。</p>
      </section>
    </main>
  );
}
